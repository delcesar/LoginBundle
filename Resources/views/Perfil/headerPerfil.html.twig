<div class="navbar blue darken-2 padding0">
    <div class="container-fluid">
        <div class="navbar-header pull-left">
            <a class="navbar-brand padding0" href="{{path('login_redirigir_homepage_nivel')}}">
                <img src="{{ asset('/assets/img/iconos/pms-text-small.png') }}" alt="PMS" width="135" height="60" />
            </a>
        </div>
        <div class="navbar-header pull-right margintop5" id="area-usuario">
            <ul class="nav navbar-nav navbar-right marginright20">
                <li class="dropdown nav-item">
                    <a href="#" class="dropdown-toggle btn btn-flat white-text" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                        <i class="mdi mdi-account mdi-inverse mdi-1x"></i>
                        {{app.session.get('nombreusuario')}}
                    </a>
                    <ul class="pull-right dropdown-menu dropdown-arrow dropdown-login-area dropdown-menu-right">
                        <li>
                            <div class="row marginbottom20 margintop20">
                                <div class="col-xs-3 p-r-0">
                                    <i class="mdi mdi-account-circle mdi-4x"></i>
                                </div>
                                <div class="col-xs-9 p-l-0">
                                    <span class="bold h4">{{app.session.get('nombreusuario')}}</span><br/>
                                    <span class="text-muted">{{app.session.get('emailusuario')}}</span><br/>
                                    <span class="text-muted small">Zona horaria: {{app.session.get('timezone')}}</span>
                                </div>
                            </div>
                        </li>
                        <li class="dropdown-divider"></li>
                        <li class="marginbottom10">
                            <div class="row">
                                <div class="col-xs-6 text-center">
                                    <a href="{{path('login_perfil_usuario')}}" class="btn btn-secondary btn-flat waves-effect">
                                        Configurar perfil
                                    </a>
                                </div>
                                <div class="col-xs-6 text-center">
                                    <a href="{{path('login_logout')}}" class="btn btn-danger btn-flat red lighten-2 waves-effect waves-light">
                                        Cerrar sesión
                                    </a>
                                </div>
                            </div>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</div>